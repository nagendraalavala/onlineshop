apply from: "../../common.gradle"

dependencies {
    compile project (":components:appusers")
    compile("org.springframework.boot:spring-boot-starter-web:$springBootVersion")
    compile('commons-lang:commons-lang:2.4')
}